# 🔍 ИНСТРУКЦИЯ ПО ИССЛЕДОВАНИЮ ФОРМЫ АВТОРИЗАЦИИ

## Цель
Получить информацию о структуре формы авторизации на login.consultant.ru для адаптации scraper.

## ⚠️ Что нам нужно найти

1. **Имена полей формы** (атрибуты `name`)
2. **URL отправки формы** (атрибут `action`)
3. **CSRF токен** (если используется)
4. **Способ проверки успешной авторизации**

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Откройте страницу авторизации

```
URL: https://login.consultant.ru/
```

### Шаг 2: Откройте DevTools

- **Windows/Linux**: Нажмите `F12` или `Ctrl+Shift+I`
- **Mac**: Нажмите `Cmd+Option+I`

### Шаг 3: Найдите форму авторизации

1. Перейдите на вкладку **Elements** (Элементы)
2. Нажмите `Ctrl+F` (или `Cmd+F` на Mac)
3. В поиске введите: `<form`
4. Должна подсветиться форма авторизации

### Шаг 4: Изучите форму

**Скопируйте мне весь HTML код формы**:

1. Правый клик на элементе `<form>` → **Copy** → **Copy outerHTML**
2. Вставьте мне весь скопированный текст

---

## 🔎 ЧТО ИСКАТЬ В HTML

### 1. Атрибуты формы

```html
<form action="???" method="???">
```

**Нам нужно:** значение `action` (куда отправляется форма)

### 2. Поле логина

```html
<input type="text" name="???" id="???" placeholder="Email/Логин">
```

**Нам нужно:** значение атрибута `name`

Типичные варианты: `login`, `username`, `email`, `user`, `loginname`

### 3. Поле пароля

```html
<input type="password" name="???" id="???" placeholder="Пароль">
```

**Нам нужно:** значение атрибута `name`

Типичные варианты: `password`, `pass`, `pwd`

### 4. CSRF токен (если есть)

```html
<input type="hidden" name="???" value="...очень длинная строка...">
```

**Нам нужно:** значение атрибута `name`

Типичные варианты: `_csrf`, `csrf_token`, `token`, `authenticity_token`

### 5. Другие скрытые поля

Могут быть дополнительные `<input type="hidden">` - их тоже нужно передать при авторизации.

---

## 🎯 АЛЬТЕРНАТИВНЫЙ МЕТОД (если не можете скопировать HTML)

### Через Network tab (DevTools)

1. Откройте DevTools → вкладка **Network** (Сеть)
2. Попробуйте авторизоваться с ТЕСТОВЫМИ данными:
   - Логин: `test@test.com`
   - Пароль: `test123`
3. В Network найдите POST запрос (обычно первый после нажатия кнопки входа)
4. Кликните на него
5. Перейдите на вкладку **Payload** (Полезная нагрузка)
6. **Скопируйте мне список полей** которые отправляются

Пример того что нужно скопировать:
```
login: test@test.com
password: test123
_csrf: abc123def456...
```

---

## ✅ ЧТО МНЕ ОТПРАВИТЬ

**Вариант 1 (предпочтительный):**
```html
<!-- Скопируйте весь HTML код формы -->
<form action="/auth/login" method="post">
  <input type="hidden" name="_csrf" value="...">
  <input type="text" name="login" placeholder="Email">
  <input type="password" name="password" placeholder="Пароль">
  <button type="submit">Войти</button>
</form>
```

**Вариант 2 (если вариант 1 не получается):**
Просто напишите мне:
```
Имя поля логина: login
Имя поля пароля: password
CSRF токен: _csrf
Action формы: /auth/login
```

---

## 🚀 ЧТО БУДЕТ ДАЛЬШЕ

После того как вы дадите мне эту информацию:

1. ✅ Я обновлю 4 TODO секции в `consultant_scraper_v2.py`
2. ✅ Мы протестируем авторизацию с вашими реальными учетными данными
3. ✅ Если авторизация успешна - переходим к поиску и скачиванию документов
4. ✅ Запускаем полный пайплайн!

---

## ❓ ВОПРОСЫ

**Q: Может ли это навредить моему аккаунту?**
A: Нет. Мы просто изучаем структуру формы. Для теста используйте неправильные данные.

**Q: Сайт требует капчу?**
A: Если да - отметьте это. Возможно придется добавить обработку капчи.

**Q: Форма использует JavaScript для отправки?**
A: Смотрите в Network tab - там видны реальные запросы которые отправляются.

---

## 📞 ГОТОВЫ?

Когда найдете информацию - просто отправьте мне HTML код формы или список полей!

Я жду! 🔍
