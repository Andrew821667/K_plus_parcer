# Руководство по полному пайплайну K_plus_parcer

**ПОЛНЫЙ АВТОМАТИЗИРОВАННЫЙ ПАЙПЛАЙН:**
1. 🌐 Авторизация на онлайн.consultant.ru
2. 🔍 Поиск документов (все кодексы РФ)
3. 📥 Скачивание PDF файлов
4. 🔧 Парсинг документов
5. 📊 Экспорт в Markdown (RAG) и JSON (ML)

---

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей

```bash
# Установка всех зависимостей включая Selenium
pip install -e .
```

### Шаг 2: Установка Chrome/Chromium

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install chromium-browser chromium-chromedriver
```

**macOS:**
```bash
brew install --cask google-chrome
brew install chromedriver
```

**Windows:**
- Скачайте Chrome с https://www.google.com/chrome/
- ChromeDriver устанавливается автоматически через webdriver-manager

### Шаг 3: Настройка учётных данных

**Вариант 1: Переменные окружения (рекомендуется)**

```bash
# Linux/macOS
export CONSULTANT_USERNAME="your_email@example.com"
export CONSULTANT_PASSWORD="your_password"

# Windows (PowerShell)
$env:CONSULTANT_USERNAME="your_email@example.com"
$env:CONSULTANT_PASSWORD="your_password"
```

**Вариант 2: Файл .env**

Создайте файл `.env` в корне проекта:
```env
CONSULTANT_USERNAME=your_email@example.com
CONSULTANT_PASSWORD=your_password
```

⚠️ **ВАЖНО:** Добавьте `.env` в `.gitignore` чтобы не закоммитить пароли!

### Шаг 4: Запуск полного пайплайна

```bash
python pipeline_full.py
```

---

## 📋 Варианты использования

### Базовый запуск

```bash
# С учётными данными из переменных окружения
python pipeline_full.py
```

### С параметрами командной строки

```bash
# Указать учётные данные явно
python pipeline_full.py \
  --username your_email@example.com \
  --password your_password
```

### Headless режим (без GUI)

```bash
# Запуск браузера в фоне (полезно для серверов)
python pipeline_full.py --headless
```

### Кастомные директории

```bash
# Указать свои директории для скачивания и результатов
python pipeline_full.py \
  --download-dir /path/to/downloads \
  --output-dir /path/to/results
```

---

## 📊 Что происходит при запуске

### Этап 1: Авторизация 🔐

```
📝 ЭТАП 1: Авторизация на сайте...
   - Открытие браузера Chrome
   - Переход на онлайн.consultant.ru
   - Заполнение формы входа
   - Ожидание успешной авторизации
✅ Авторизация успешна
```

### Этап 2: Скачивание 📥

```
📥 ЭТАП 2: Поиск и скачивание кодексов...
   [1/20] Гражданский кодекс часть 1 → скачивание...
   [2/20] Гражданский кодекс часть 2 → скачивание...
   [3/20] Уголовный кодекс → скачивание...
   ...
✅ Скачано файлов: 20
```

**Скачиваемые кодексы:**
- Гражданский кодекс РФ (4 части)
- Уголовный кодекс РФ
- Налоговый кодекс РФ (2 части)
- Трудовой кодекс РФ
- Семейный кодекс РФ
- КоАП РФ
- АПК РФ, ГПК РФ, УПК РФ
- Земельный кодекс РФ
- Жилищный кодекс РФ
- Бюджетный кодекс РФ
- Градостроительный кодекс РФ
- Водный кодекс РФ
- Лесной кодекс РФ
- Воздушный кодекс РФ

### Этап 3: Парсинг 🔧

```
🔧 ЭТАП 3: Парсинг скачанных PDF...
   [1/20] Обработка: Гражданский_кодекс_часть_1.pdf
      ✅ ФЕДЕРАЛЬНЫЙ ЗАКОН N 51-ФЗ, 7 глав, 453 статьи
   [2/20] Обработка: Уголовный_кодекс.pdf
      ✅ ФЕДЕРАЛЬНЫЙ ЗАКОН N 63-ФЗ, 6 глав, 360 статей
   ...
```

### Этап 4: Итоговая статистика 📊

```
================================================================================
📊 ИТОГОВАЯ СТАТИСТИКА ПОЛНОГО ПАЙПЛАЙНА
================================================================================
Скачано PDF:         20 ✅
Распарсено успешно:  20 ✅
Ошибок:              0 ❌
Время работы:        180.5s (3.0 мин)

📁 Результаты:
   Markdown (RAG):  data/output/markdown/
   JSON (ML):       data/output/json/
   Отчёты:          data/output/reports/
================================================================================
```

---

## 📁 Структура результатов

После завершения пайплайна:

```
K_plus_parcer/
├── data/
│   ├── input/
│   │   └── codex/
│   │       ├── Гражданский_кодекс_часть_1.pdf
│   │       ├── Гражданский_кодекс_часть_2.pdf
│   │       ├── Уголовный_кодекс.pdf
│   │       └── ... (все скачанные PDF)
│   │
│   └── output/
│       ├── markdown/              ⭐ ПРИОРИТЕТ #1 - для RAG систем
│       │   ├── Гражданский_кодекс_часть_1.md
│       │   ├── Уголовный_кодекс.md
│       │   └── ...
│       │
│       ├── json/                  📊 Для ML обучения
│       │   ├── Гражданский_кодекс_часть_1.json
│       │   ├── Уголовный_кодекс.json
│       │   └── ...
│       │
│       └── reports/               📝 Отчёты
│           └── full_pipeline_20251218_120000.json
```

---

## 🔧 Настройка под ваш сайт

### Важно: Селекторы могут измениться!

Файл `src/scraper/consultant_scraper.py` содержит селекторы HTML элементов сайта.
Если сайт обновится, нужно будет обновить селекторы.

**Основные селекторы:**

```python
# Авторизация
username_field = By.ID, "username"
password_field = By.ID, "password"
login_button = By.CSS_SELECTOR, "button[type='submit']"

# Поиск
search_field = By.ID, "search-query"
search_button = By.CSS_SELECTOR, "button[type='submit']"

# Результаты
result_elements = By.CSS_SELECTOR, ".search-result-item"

# Скачивание PDF
pdf_button = By.XPATH, "//a[contains(text(), 'PDF')]"
```

### Как узнать актуальные селекторы:

1. Откройте онлайн.consultant.ru в Chrome
2. Нажмите F12 (DevTools)
3. Используйте инспектор (Ctrl+Shift+C)
4. Наведите на нужный элемент
5. Скопируйте селектор (ПКМ → Copy → Copy selector)

---

## 🐛 Устранение проблем

### Проблема: "chromedriver not found"

**Решение:**
```bash
pip install webdriver-manager --upgrade
```

WebDriver Manager автоматически скачает нужный драйвер.

### Проблема: "Session not created"

**Причина:** Несовместимость версий Chrome и ChromeDriver

**Решение:**
```bash
# Обновите Chrome до последней версии
# WebDriver Manager подберёт совместимый драйвер
pip install webdriver-manager --upgrade
```

### Проблема: Авторизация не проходит

**Проверьте:**
1. Правильность учётных данных
2. Активна ли подписка на онлайн.consultant.ru
3. Не требуется ли капча или 2FA

**Для отладки:**
```bash
# Запустите без headless чтобы видеть браузер
python pipeline_full.py  # (headless по умолчанию выключен)
```

### Проблема: PDF не скачиваются

**Проверьте:**
1. Права на запись в `data/input/codex/`
2. Достаточно ли места на диске
3. Не блокирует ли антивирус скачивание

**Логи:**
```bash
# Проверьте логи
cat logs/k_plus_parcer.log | grep ERROR
```

### Проблема: Headless режим не работает на сервере

**Linux сервер без GUI:**
```bash
# Установите виртуальный дисплей
sudo apt install xvfb

# Запустите с xvfb
xvfb-run python pipeline_full.py --headless
```

---

## 🔒 Безопасность

### Защита учётных данных

**❌ НИКОГДА:**
- Не коммитьте пароли в Git
- Не храните пароли в коде
- Не передавайте пароли в URL

**✅ ПРАВИЛЬНО:**
```bash
# Используйте переменные окружения
export CONSULTANT_USERNAME="..."
export CONSULTANT_PASSWORD="..."

# Или файл .env (добавьте в .gitignore!)
echo ".env" >> .gitignore
```

### .gitignore для паролей

Убедитесь что `.gitignore` содержит:
```
.env
.env.local
credentials.json
passwords.txt
```

---

## 📈 Производительность

### Ожидаемое время работы

| Этап | Время |
|------|-------|
| Авторизация | 5-10 сек |
| Скачивание 1 кодекса | 10-30 сек |
| Скачивание 20 кодексов | 3-10 мин |
| Парсинг 1 кодекса | 1-5 сек |
| Парсинг 20 кодексов | 30-120 сек |
| **ИТОГО (20 кодексов)** | **4-12 минут** |

### Оптимизация

**Для ускорения:**
```bash
# Headless режим быстрее
python pipeline_full.py --headless

# Параллельный парсинг (если нужно)
# Скачайте сначала все PDF, затем парсите отдельно:
python pipeline_codex.py  # Только парсинг готовых PDF
```

---

## 🎯 Следующие шаги после обработки

### 1. Для RAG систем

Используйте Markdown файлы:

```python
from langchain.text_splitter import MarkdownHeaderTextSplitter
from langchain.vectorstores import Chroma
from langchain.embeddings import HuggingFaceEmbeddings

# Загрузка Markdown
with open('data/output/markdown/Гражданский_кодекс_часть_1.md') as f:
    content = f.read()

# Chunking
splitter = MarkdownHeaderTextSplitter(
    headers_to_split_on=[
        ("#", "Документ"),
        ("##", "Глава"),
        ("###", "Статья"),
    ]
)
chunks = splitter.split_text(content)

# Embeddings
embeddings = HuggingFaceEmbeddings(
    model_name="sentence-transformers/paraphrase-multilingual-mpnet-base-v2"
)

# Vector DB
vectorstore = Chroma.from_documents(chunks, embeddings)
```

### 2. Для ML обучения

Используйте JSON файлы:

```python
import json

# Загрузка структурированных данных
with open('data/output/json/Уголовный_кодекс.json') as f:
    data = json.load(f)

# Создание training examples
for chapter in data['structure']['chapters']:
    for article in chapter['articles']:
        # Формируйте instruction-following examples
        example = {
            "instruction": f"Что говорит {article['title']}?",
            "input": "",
            "output": article['full_text']
        }
```

---

## 📞 Поддержка

**Логи:** `logs/k_plus_parcer.log`
**Отчёты:** `data/output/reports/full_pipeline_*.json`
**Issues:** https://github.com/Andrew821667/K_plus_parcer/issues

---

## ✅ Чек-лист готовности

- [ ] Python 3.8+ установлен
- [ ] Chrome/Chromium установлен
- [ ] Зависимости установлены (`pip install -e .`)
- [ ] Учётные данные настроены (переменные окружения или .env)
- [ ] Активна подписка на онлайн.consultant.ru
- [ ] Директории созданы (`data/input/codex/`, `data/output/`)
- [ ] Права на запись есть

**Готовы к запуску! 🚀**

```bash
python pipeline_full.py
```
