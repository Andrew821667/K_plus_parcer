# Установка и запуск K_plus_parcer на локальной машине

Инструкция для обработки кодексов РФ на вашем компьютере.

## 📋 Системные требования

- **Python:** 3.8 или выше
- **ОС:** Windows, macOS или Linux
- **Место на диске:** ~500 MB для зависимостей + место для PDF файлов
- **RAM:** Минимум 2 GB (рекомендуется 4+ GB для больших документов)

## 🚀 Пошаговая установка

### Шаг 1: Установка Python (если ещё не установлен)

**Windows:**
1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. Запустите установщик
3. ✅ **ВАЖНО:** Отметьте "Add Python to PATH"
4. Установите

**macOS:**
```bash
# Используя Homebrew
brew install python@3.11
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install python3 python3-pip python3-venv
```

Проверьте установку:
```bash
python --version
# Должно быть: Python 3.8+
```

### Шаг 2: Клонирование репозитория

Откройте терминал (Command Prompt на Windows) и выполните:

```bash
# Перейдите на рабочий стол
cd ~/Desktop           # macOS/Linux
cd %USERPROFILE%\Desktop   # Windows

# Клонируйте репозиторий
git clone https://github.com/Andrew821667/K_plus_parcer.git

# Перейдите в директорию проекта
cd K_plus_parcer
```

### Шаг 3: Создание виртуального окружения

**Рекомендуется** для изоляции зависимостей:

```bash
# Создание виртуального окружения
python -m venv venv

# Активация
# macOS/Linux:
source venv/bin/activate

# Windows:
venv\Scripts\activate

# После активации вы увидите (venv) в начале строки терминала
```

### Шаг 4: Установка зависимостей

```bash
# Обновление pip
pip install --upgrade pip

# Установка проекта в режиме разработки
pip install -e .

# Проверка установки
python -c "from src import NPAParser; print('✅ Установка успешна!')"
```

### Шаг 5: Подготовка директорий

```bash
# Создание структуры директорий для кодексов
mkdir -p data/input/codex
mkdir -p data/output
```

### Шаг 6: Размещение PDF файлов кодексов

Поместите PDF файлы всех кодексов РФ в директорию:
```
K_plus_parcer/data/input/codex/
```

**Пример структуры:**
```
K_plus_parcer/
└── data/
    └── input/
        └── codex/
            ├── ГК_РФ_часть1.pdf
            ├── ГК_РФ_часть2.pdf
            ├── ГК_РФ_часть3.pdf
            ├── ГК_РФ_часть4.pdf
            ├── УК_РФ.pdf
            ├── НК_РФ_часть1.pdf
            ├── НК_РФ_часть2.pdf
            ├── ТК_РФ.pdf
            ├── СК_РФ.pdf
            ├── КоАП_РФ.pdf
            ├── АПК_РФ.pdf
            ├── ГПК_РФ.pdf
            ├── УПК_РФ.pdf
            └── ... (остальные кодексы)
```

**Где взять PDF кодексов:**
- КонсультантПлюс (если есть доступ)
- Официальные правовые порталы
- pravo.gov.ru

## 🏃 Запуск пайплайна

### Базовый запуск

```bash
# Убедитесь что виртуальное окружение активно (venv)
python pipeline_codex.py
```

### Запуск с параметрами

```bash
# Указать кастомные директории
python pipeline_codex.py --input data/input/codex --output data/output

# Отключить Markdown режим (быстрее, но менее точно)
python pipeline_codex.py --no-markdown-mode

# Отключить очистку текста
python pipeline_codex.py --no-clean-text
```

### Что происходит во время обработки:

```
================================================================================
🚀 ПАЙПЛАЙН ОБРАБОТКИ КОДЕКСОВ РФ
================================================================================
Входная директория: data/input/codex
Выходная директория: data/output
Markdown режим: Да
Очистка текста: Да
================================================================================

📚 Найдено документов: 15

[1/15] Обработка: ГК_РФ_часть1.pdf
   ✅ Успешно (12.3s)

[2/15] Обработка: УК_РФ.pdf
   ✅ Успешно (8.7s)

...
```

## 📊 Результаты обработки

После завершения вы получите:

```
data/output/
├── markdown/              ⭐ ПРИОРИТЕТ #1 - База знаний для RAG
│   ├── ГК_РФ_часть1.md
│   ├── УК_РФ.md
│   └── ...
├── json/                  📊 Структурированные данные для ML
│   ├── ГК_РФ_часть1.json
│   ├── УК_РФ.json
│   └── ...
└── reports/               📝 Отчёты о обработке
    └── report_20251218_120000.json
```

### Структура Markdown файла (для RAG):

```markdown
---
doc_type: ФЕДЕРАЛЬНЫЙ ЗАКОН
number: 63-ФЗ
date: '1994-12-21'
title: Гражданский кодекс Российской Федерации (часть первая)
status: действующий
categories: []
references: []
source: КонсультантПлюс
---

# ФЕДЕРАЛЬНЫЙ ЗАКОН от 21.12.1994 N 63-ФЗ

**Гражданский кодекс Российской Федерации (часть первая)**

## Глава 1. ОСНОВНЫЕ ПОЛОЖЕНИЯ

### Статья 1. Основные начала гражданского законодательства

1. Гражданское законодательство основывается на признании...
...
```

## 🔧 Проверка работы на одном файле

Перед массовой обработкой рекомендуется протестировать на одном кодексе:

```bash
# Создайте тестовую директорию
mkdir -p data/input/test

# Скопируйте один PDF
cp data/input/codex/УК_РФ.pdf data/input/test/

# Запустите на тестовой директории
python pipeline_codex.py --input data/input/test --output data/output/test
```

## 🐛 Устранение проблем

### Проблема: "ModuleNotFoundError: No module named 'src'"

**Решение:**
```bash
pip install -e .
```

### Проблема: "Permission denied" при создании директорий

**Решение (Linux/macOS):**
```bash
sudo chown -R $USER:$USER ~/Desktop/K_plus_parcer
```

### Проблема: PDF файлы не найдены

**Проверьте:**
```bash
ls -la data/input/codex/
```

Убедитесь что файлы имеют расширение `.pdf` (не `.PDF`)

### Проблема: Недостаточно памяти

**Для очень больших документов:**
```bash
# Обрабатывайте по одному
python pipeline_codex.py --input data/input/codex/УК_РФ.pdf
```

## 📈 Мониторинг прогресса

Логи сохраняются в:
```
logs/k_plus_parcer.log
```

Просмотр в реальном времени:
```bash
# Linux/macOS
tail -f logs/k_plus_parcer.log

# Windows (PowerShell)
Get-Content logs/k_plus_parcer.log -Wait
```

## 🎯 Следующие шаги после обработки

### 1. Для RAG систем:

Используйте Markdown файлы из `data/output/markdown/`:

```python
from langchain.text_splitter import MarkdownHeaderTextSplitter
from langchain.embeddings import HuggingFaceEmbeddings
from langchain.vectorstores import Chroma

# Загрузка Markdown
with open('data/output/markdown/УК_РФ.md', 'r') as f:
    md_content = f.read()

# Chunking по заголовкам
splitter = MarkdownHeaderTextSplitter(
    headers_to_split_on=[
        ("#", "Документ"),
        ("##", "Глава"),
        ("###", "Статья"),
    ]
)
chunks = splitter.split_text(md_content)

# Создание embeddings
embeddings = HuggingFaceEmbeddings(
    model_name="sentence-transformers/paraphrase-multilingual-mpnet-base-v2"
)

# Загрузка в векторную БД
vectorstore = Chroma.from_documents(chunks, embeddings)
```

### 2. Для ML обучения:

Используйте JSON файлы из `data/output/json/`:

```python
import json

# Загрузка JSON
with open('data/output/json/УК_РФ.json', 'r') as f:
    data = json.load(f)

# Извлечение статей для обучения
articles = data['structure']['chapters']
# Формируйте training examples...
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в `logs/k_plus_parcer.log`
2. Посмотрите отчёт в `data/output/reports/`
3. Создайте issue на GitHub: https://github.com/Andrew821667/K_plus_parcer/issues

## ✅ Чек-лист готовности

- [ ] Python 3.8+ установлен
- [ ] Git установлен
- [ ] Репозиторий склонирован на рабочий стол
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены (`pip install -e .`)
- [ ] Директории созданы (`data/input/codex/`, `data/output/`)
- [ ] PDF файлы кодексов размещены в `data/input/codex/`
- [ ] Тестовый запуск прошёл успешно

**Готовы к обработке! 🚀**

```bash
python pipeline_codex.py
```
